<form method="post" action="{{ path('app_actuality_delete', {'id': actuality.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette bannière?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actuality.id) }}">
    <button class="btn btn-danger text-light">Supprimer</button>
</form>
