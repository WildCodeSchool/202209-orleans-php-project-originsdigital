{% extends 'layoutAdmin.html.twig' %}

{% block title %}Admin | Modification catégorie{% endblock %}

{% block body %}
    <div class="video-show container-fluid p-3">
		<h1 class="text-center m-3">{{ "modifier une catégorie"|upper }}</h1>
		<div class="delete-button mx-5 my-4 ps-5 ">
			{{ include('admin_cat/_delete_form.html.twig') }}
		</div>
		<div class="d-grid gap-2 col-10 py-1 mx-auto pb-3">
			{{ include('admin_cat/_form.html.twig', {'button_label': 'Modifier la catégorie'}) }}
		</div>
	</div>
{% endblock %}
