{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn {{ button_class|default('btn-primary') }} mt-3 me-3">{{ button_label|default('Ajouter') }}</button>
<a class="btn btn-dark mt-3" href="{{ path('app_ad_index') }}">Retour</a>
{{ form_end(form) }}
