{% extends 'base.html.twig' %}

{% block title %}Sponsor{% endblock %}

{% block body %}
    <h1>Sponsor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sponsor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sponsor.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ sponsor.logo }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ sponsor.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_sponsor_index') }}">back to list</a>

    <a href="{{ path('app_admin_sponsor_edit', {'id': sponsor.id}) }}">edit</a>

    {{ include('admin_sponsor/_delete_form.html.twig') }}
{% endblock %}
